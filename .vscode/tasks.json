{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "SessionServer",
            "type": "shell",
            "command": "make",
            "options": {
                "cwd": "${workspaceFolder}/SessionServer"  // 指定当前工作目录
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}
